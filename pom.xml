<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<groupId>at.sti2.iris</groupId>
	<artifactId>iris</artifactId>
	<version>0.7.2</version>
	<packaging>pom</packaging>
	<name>IRIS</name>
	<description>Integrated Rule Inference System is an extensible reasoning engine for expressive rule-based languages.</description>

	<modules>
		<module>iris-api</module>
		<module>iris-impl</module>
		<module>iris-parser</module>
    	<module>iris-impl-distributed</module>
  </modules>

	<properties>
		<compiler.version>1.6</compiler.version>
		<source.encoding>UTF-8</source.encoding>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
	</properties>

	<scm>
		<connection>scm:svn:https://iris-reasoner.svn.sourceforge.net/svnroot/iris-reasoner/iris/tags/iris-0.7.2</connection>
		<developerConnection>scm:svn:https://iris-reasoner.svn.sourceforge.net/svnroot/iris-reasoner/iris/tags/iris-0.7.2</developerConnection>
		<url>http://iris-reasoner.svn.sourceforge.net/viewvc/iris-reasoner/iris/tags/iris-0.7.2</url>
	</scm>
	
	<build>
		<plugins>
			<!--
				Maven dixit: "We are using the verify phase here because it is the
				phase that comes before the install phase, thus making sure that the
				sources jar has been created before the install takes place."
			-->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-source-plugin</artifactId>
				<executions>
					<execution>
						<id>attach-sources</id>
						<phase>verify</phase>
						<goals>
							<goal>jar</goal>
						</goals>
					</execution>
				</executions>
				<configuration>
					<encoding>${source.encoding}</encoding>
				</configuration>
			</plugin>

			<!--
				Maven dixit: "The Compiler Plugin is used to compile the sources of
				your project. The default compiler is javac and is used to compile
				Java sources. The default source setting is 1.3 and the default
				target setting is 1.1, independently of the JDK you run Maven with.
				If you want to change these defaults, you should set source and
				target [...] "
			-->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<configuration>
					<encoding>${source.encoding}</encoding>
					<source>${compiler.version}</source>
					<target>${compiler.version}</target>
					<verbose>false</verbose>
					<showWarnings>false</showWarnings>
				</configuration>
			</plugin>
		</plugins>
	</build>

	<distributionManagement>		
		<repository>
			<id>sti2-archiva-external</id>
			<name>STI Innsbruck External Release Repository</name>
			<url>http://maven.sti2.at/archiva/repository/external</url>
			<uniqueVersion>false</uniqueVersion>
		</repository>
		<snapshotRepository>
			<id>sti2-archiva-snapshots</id>
			<name>STI Innsbruck Snapshot Repository</name>
			<url>http://maven.sti2.at/archiva/repository/snapshots</url>
			<uniqueVersion>false</uniqueVersion>
		</snapshotRepository>
	</distributionManagement>

	<repositories>
		<repository>
  			<id>conjars.org</id>
  			<url>http://conjars.org/repo</url>
  		</repository>
		<repository>
			<id>sti2-archiva-external</id>
			<url>http://maven.sti2.at/archiva/repository/external</url>
		</repository>
		<repository>
			<id>sti2-archiva-snapshots</id>
			<url>http://maven.sti2.at/archiva/repository/snapshots</url>
		</repository>
		 <repository>
           <id>semweb4j-repo</id>
           <name>Semweb4j.org maven repo</name>
           <url>http://semweb4j.org/repo/</url>
           <releases>
               <enabled>true</enabled>
           </releases>
       </repository>  
    
	</repositories>
</project>